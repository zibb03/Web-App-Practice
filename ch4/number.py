# 인자 값이 양수일 경우, 출력하는 문제

# number라는 인자를 받아 실행되는 judge 함수
def judge(number):
    # number 변수의 값이 양수일 때
    if number > 0:
        # number 변수의 값을 출력
        print(number)

# 정의한 judge 함수를 실행하는 코드
# 인자를 전달해 주지 않으면 실행되지 않음
judge(97235)